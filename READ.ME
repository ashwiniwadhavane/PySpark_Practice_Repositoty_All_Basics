# PySpark Practice

This repository contains my hands-on practice with **PySpark** using **Databricks**.  
I created this notebook to learn and practice different PySpark DataFrame operations, transformations, functions, joins, window functions, data writing, and Spark SQL concepts.

## 📌 Topics Covered

### 1. Data Reading
- Reading JSON files
- Reading CSV files
- Using `header=True`
- Using `inferSchema=True`

### 2. Schema
- Automatic schema inference
- DDL schema definition
- `StructType`
- `StructField`
- PySpark data types

### 3. DataFrame Operations
- `select()`
- Column aliasing using `alias()`
- `filter()`
- `withColumn()`
- `withColumnRenamed()`
- `lit()`
- `regexp_replace()`

### 4. Type Casting
Practiced converting column data types using:
- `StringType()`
- `DoubleType()`
- `cast()`

### 5. Sorting and Limiting
- `sort()`
- Ascending and descending sorting
- Sorting using multiple columns
- `limit()`

### 6. Removing Data
- `drop()`
- `dropDuplicates()`
- `distinct()`

### 7. Union Operations
- `union()`
- `unionByName()`

### 8. String Functions
- `initcap()`
- `upper()`

### 9. Date Functions
- `current_date()`
- `date_add()`
- `datediff()`
- `date_format()`

### 10. Handling Null Values
- `dropna()`
- `fillna()`
- Handling null values for specific columns

### 11. Array Operations
- `split()`
- Array indexing
- `explode()`
- `array_contains()`

### 12. Aggregations
- `groupBy()`
- `sum()`
- `avg()`
- `agg()`
- `pivot()`

### 13. Collection Functions
- `collect_list()`

### 14. Joins
Practiced different types of joins:
- Inner Join
- Left Join
- Right Join
- Anti Join

### 15. Window Functions
- `row_number()`
- `rank()`
- `dense_rank()`
- Window specification
- Cumulative sum
- `rowsBetween()`
- `unboundedPreceding`
- `currentRow`

### 16. User Defined Function
- Creating a Python function
- Creating a PySpark UDF
- Applying UDFs to DataFrame columns

### 17. Data Writing
Practiced different write modes:
- `append`
- `overwrite`
- `error`
- `ignore`

Also practiced writing data in:
- CSV
- Parquet

### 18. Spark SQL
- Creating temporary views
- Running SQL queries on DataFrames
- Filtering data using SQL
- Converting SQL results back to DataFrames

## 🛠️ Technologies Used

- Python
- PySpark
- Apache Spark
- Databricks
- Spark SQL

## 📊 Dataset

The main practice dataset used in this notebook is the **BigMart Sales dataset**.


## 🎯 Purpose

The purpose of this repository is to document my learning and hands-on practice with PySpark and Apache Spark.

Through this practice, I worked with DataFrame transformations, data cleaning, aggregations, joins, window functions, data types, file formats, and Spark SQL.



